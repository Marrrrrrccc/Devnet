name: Test Program
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkoutev2
    - name: Super-Linter
      uses: github/super-linter@v2.1.e

    - name: run unit tests [pytest]
      run: pytest      


