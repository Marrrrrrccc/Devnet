name: Test Program
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: run linter[pylint]
        run: pylint .

      - name: run unit tests [pytest]
        run: pytest      


